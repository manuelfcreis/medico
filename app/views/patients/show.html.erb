<div class="show-div patient">
  <div class="left-bar">
    <%= render 'shared/profile', locals: { user: @doctor } %>
    <% if current_class == 'patient' %>
    <div class="medications">
      medications
      <%#= render 'shared/medications', locals: { user: user } %>
    </div>
    <% else %>
    <div class="link-list">
      <ul class="list-group">
        <li class="list-group-item">
          <a href="#">Add a Prescription</a>
        </li>
        <li class="list-group-item">
          <a href="#">Manage your appointments</a>
        </li>
        <li class="list-group-item">
          <a href="#">Search for a Medication</a>
        </li>
      </ul>
    </div>
    <% end %>
  </div>

  <div class="center-bar">
    <div class="chatroom">
      <%= render 'messages/messages' %>
    </div>
  </div>


  <div class="right-top-bar">
    <%= render 'shared/appointments', locals: { user: @doctor } %>
  </div>
  <div class="right-bottom-bar">
    <%= render 'shared/documents', locals: { user: @doctor } %>
  </div>
</div>
